import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    rootContainer: {
        flex: 1
    },
    gradientContainer: {
        height: '100%'
    },
    image: {
        height: '45%',
        width: '100%',
        opacity: 0.5
    }
});

export default styles;
