import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    rootContainer: {
        flex: 1,
        marginTop: '10%',
    },
    parent: {
        flex: 1,

    }
});

export default styles;
