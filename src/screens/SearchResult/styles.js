import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    rootContainer: {
        flex: 1,
        marginTop: '10%',
    },
    parent: {
        flex: 1
    },
    image: {
        height: 200,
        width: 400,
    }
});

export default styles;
